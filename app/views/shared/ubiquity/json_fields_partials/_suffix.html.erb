
<label class="control-label multi_value optional" for="#{template}_#{metadata_field}_suffix">
  <%= t('hyrax.labels.suffix') %></label>
<%= text_field_tag  "#{template}[#{metadata_field}_group][][#{metadata_field}_suffix]",  hash.dig("#{metadata_field}_suffix"),
                    class: "#{template}_#{metadata_field}_suffix #{metadata_field}_suffix form-control multi-text-field multi_value ubiquity_#{metadata_field}_suffix",
                    placeholder: '',
                    name: "#{template}[#{metadata_field}_group][][#{metadata_field}_suffix]",
                    data: {field_name:"ubiquity_#{metadata_field}_suffix"}
%>
<br>
