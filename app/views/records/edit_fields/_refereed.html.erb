<% tenant_work_settings =  get_tenant_work_settings  %>

<%= f.input :refereed, as: :select,
            collection: RefereedService.select_options,
            include_blank: true,
            input_html: { class: 'form-control' },
            hint: tenant_work_settings['help_texts']['refereed'] %>
