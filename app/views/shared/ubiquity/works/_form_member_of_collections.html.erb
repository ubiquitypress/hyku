<!-- Copied from Hyrax https://github.com/samvera/hyrax/blob/v2.0.2/app/views/hyrax/base/_form_member_of_collections.html.erb -->
<!-- rendered from app/views/shared/ubiquity/works/_form_relationships.html.erb-->

<h2><%= t("hyrax.works.form.in_collections") %></h2>
<div id="collection-widget">
   <% if current_ability.admin? %>
     <%= f.input :member_of_collection_ids,
              as: :select,
              collection: f.object.collections_for_select,
              selected: f.object.member_of_collections(params[:add_works_to_collection]),
              input_html: { class: 'form-control', multiple: true } %>
    <% elsif current_user.present?  & !current_ability.admin? %>
         <%= f.input :member_of_collection_ids,
              as: :select,
              collection: f.object.public_collections_for_select,
              selected: f.object.member_of_collections(params[:add_works_to_collection]),
              input_html: { class: 'form-control', multiple: true } %>
    <% end %>
</div>
